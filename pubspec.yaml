name: intelicity_auth_microapp_flutter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  amplify_auth_cognito: ^1.6.0
  amplify_flutter: ^1.6.0
  flutter_mobx: ^2.2.0
  mobx: ^2.2.1
  dartz: ^0.10.1
  flutter_modular: ^6.3.2
  logger: ^2.0.2+1
  string_validator: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true